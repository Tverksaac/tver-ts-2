
CTver состоит из главной части и компонентов. Компонентов создаются использую данные инструменты tver'ом. Будут встроенные компоненты:
1. Passive
2. CompoundEffect
3. Damage
4. Skill

Я хочу сделать систему компонентов такой, чтобы я легко мог воплотить все эьти 4 компонента. Если их воплощение будет успешно, значит
созданных инструментов будет достаточно для создания чего угодно

Нужно сильно обдумать это.

Будет базовый компонент Character. Возможно всё будет отталкиваться от него.
Может он будет не компонентом, а основой.
 Надо начинать

На клиенте не будет компонентов. Клиент просто будет запрашивать информацию о себе.

как примерно будет выглядеть к примеру CompoundEffect
Stun extends Component {
    key = 'stun'


}

const tver_char = sda
const stat_manager = tver_char.GetPort("StatManager")

const health = new Health extends Stat(asdasdsdas)
health - Component

А ещё можно будет через Factory
то есть
Stat.Factory(sadasdas)
